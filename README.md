# API

SELECT SNOWFLAKE.CORTEX.COMPLETE(
  'mistral-large2',
  'Return ONLY the word OK.'
) AS cortex_test;


-- Create database
CREATE DATABASE IF NOT EXISTS CORTEX_POC;

-- Create schemas
CREATE SCHEMA IF NOT EXISTS CORTEX_POC.INSIGHTS;
CREATE SCHEMA IF NOT EXISTS CORTEX_POC.ANOMALY;
CREATE SCHEMA IF NOT EXISTS CORTEX_POC.CONFIG;
CREATE SCHEMA IF NOT EXISTS CORTEX_POC.MONITORING;

-- Use the new database
USE DATABASE CORTEX_POC;

-- Use Insights schema
USE SCHEMA INSIGHTS;
